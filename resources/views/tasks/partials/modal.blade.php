<dialog class="app-modal" id="task-modal">
    <div class="modal-card"><div class="modal-head"><div><h2 id="task-modal-title">Tạo task mới</h2><p>Nhập thông tin và phân công công việc cho thành viên.</p></div><button type="button" data-modal-close><i data-lucide="x"></i></button></div>
    <form id="task-modal-form" method="post" action="{{ route('tasks.store') }}">@csrf<input id="task-method" type="hidden" name="_method" value="PUT" disabled>
        <div class="modal-body form-grid"><label class="span-2">Tên công việc<input name="title" required placeholder="Nhập tên công việc"></label><label>Dự án<select name="project_id" required>@foreach($projects as $project)<option value="{{ $project->id }}">{{ $project->key }} — {{ $project->name }}</option>@endforeach</select></label><label>Người thực hiện<select name="assignee_id"><option value="">Chưa phân công</option>@foreach($users as $user)<option value="{{ $user->id }}">{{ $user->name }}</option>@endforeach</select></label><label>Trạng thái<select name="status">@foreach(['todo'=>'Việc cần làm','in_progress'=>'Đang thực hiện','review'=>'Đang review','done'=>'Hoàn thành'] as $value=>$label)<option value="{{ $value }}">{{ $label }}</option>@endforeach</select></label><label>Ưu tiên<select name="priority">@foreach(['urgent'=>'Khẩn cấp','high'=>'Cao','medium'=>'Trung bình','low'=>'Thấp'] as $value=>$label)<option value="{{ $value }}">{{ $label }}</option>@endforeach</select></label><label>Hạn hoàn thành<input type="date" name="due_date"></label><label class="span-2">Mô tả<textarea name="description" rows="4" placeholder="Mô tả chi tiết công việc"></textarea></label></div>
        <div class="modal-actions"><button class="btn danger modal-delete" id="task-delete" type="button" hidden><i data-lucide="trash-2"></i>Xóa task</button><span class="modal-spacer"></span><button class="btn secondary" type="button" data-modal-close>Hủy</button><button class="btn primary" type="submit"><i data-lucide="save"></i><span id="task-submit-label">Tạo task</span></button></div>
    </form></div>
</dialog>
