<div class="project-pagination"><span>Hiển thị {{ $paginator->firstItem()??0 }}–{{ $paginator->lastItem()??0 }} trong {{ $paginator->total() }} dự án</span>@if($paginator->hasPages())<nav class="pager-buttons">@if($paginator->onFirstPage())<span class="disabled"><i data-lucide="chevron-left"></i></span>@else<a href="{{ $paginator->previousPageUrl() }}"><i data-lucide="chevron-left"></i></a>@endif @foreach(range(1,$paginator->lastPage()) as $page)<a class="{{ $page===$paginator->currentPage()?'active':'' }}" href="{{ $paginator->url($page) }}">{{ $page }}</a>@endforeach @if($paginator->hasMorePages())<a href="{{ $paginator->nextPageUrl() }}"><i data-lucide="chevron-right"></i></a>@else<span class="disabled"><i data-lucide="chevron-right"></i></span>@endif</nav>@endif<form method="get" class="per-page-form">@foreach(request()->except(['page','per_page']) as $key=>$value)<input type="hidden" name="{{ $key }}" value="{{ $value }}">@endforeach<select name="per_page" data-auto-submit>@foreach([6,12,24] as $size)<option value="{{ $size }}" @selected($perPage===$size)>{{ $size }} / trang</option>@endforeach</select></form></div>
