<dialog class="app-modal" id="project-modal"><div class="modal-card"><div class="modal-head"><div><h2 id="project-modal-title">Tạo dự án mới</h2><p>Thiết lập thông tin, thời gian và thành viên dự án.</p></div><button type="button" data-modal-close><i data-lucide="x"></i></button></div><form id="project-modal-form" method="post" action="{{ route('projects.store') }}">@csrf<input id="project-method" type="hidden" name="_method" value="PUT" disabled><div class="modal-body form-grid"><label class="span-2">Tên dự án<input name="name" required placeholder="Nhập tên dự án"></label><label>Mã dự án<input name="key" required maxlength="12" placeholder="WEB"></label><label>Chủ dự án<select name="owner_id" required>@foreach($users as $user)<option value="{{ $user->id }}">{{ $user->name }}</option>@endforeach</select></label><label>Trạng thái<select name="status">@foreach(['planning'=>'Lên kế hoạch','active'=>'Đang chạy','on_hold'=>'Tạm dừng','completed'=>'Hoàn thành'] as $value=>$label)<option value="{{ $value }}">{{ $label }}</option>@endforeach</select></label><label>Ưu tiên<select name="priority">@foreach(['low'=>'Thấp','medium'=>'Trung bình','high'=>'Cao','urgent'=>'Khẩn cấp'] as $value=>$label)<option value="{{ $value }}">{{ $label }}</option>@endforeach</select></label><label>Ngày bắt đầu<input type="date" name="start_date"></label><label>Hạn hoàn thành<input type="date" name="due_date"></label><label class="span-2">Mô tả<textarea name="description" rows="3"></textarea></label><label class="span-2">Thành viên<select name="members[]" multiple size="4">@foreach($users as $user)<option value="{{ $user->id }}">{{ $user->name }} · {{ $user->email }}</option>@endforeach</select></label></div><div class="modal-actions"><button class="btn secondary" type="button" data-modal-close>Hủy</button><button class="btn primary" type="submit"><i data-lucide="save"></i><span id="project-submit-label">Tạo dự án</span></button></div></form></div></dialog>
